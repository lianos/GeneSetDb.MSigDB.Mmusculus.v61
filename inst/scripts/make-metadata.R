# Generates inst/exdata/metadata.csv, a file that has one row per resource to be
# added to the AnnotationHub database. Required column names and data types are
# specified in AnnotationHub::readMetadataFromCsv

# Run AnnotationHubData:::makeAnnotationHubMetadata() on your package to confirm
# the data in inst/extdata/metadata.csv are valid.